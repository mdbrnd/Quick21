{
  "name": "quick21",
  "version": "1.0.0",
  "description": "## Overview",
  "scripts": {
    "start": "node server/dist/src/server.js",
    "dev": "nodemon server/src/server.ts",
    "prebuild": "cd client && npm install && cd .. && cd server && npm install",
    "build": "cd client && npm run build && cd .. && cd server && npm run build",
    "test": "cd server && jest",
    "start:dev": "npm run build && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
